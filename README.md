# peptideBuilder
This is a plug-in for VMD (requires autopsf). 
The code allows to build a peptide ab initio just from a sequence and the corresponding secondary structure or backbone internal coordinates.
